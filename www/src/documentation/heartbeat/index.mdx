---
name: Heartbeat
route: /documentation/heartbeat
parent: Documentation
---

import { Playground } from 'docz'
import Heartbeat from '../../../../packages/heartbeat'

# heartbeat

First start off by installing the `heartbeat` package via yarn

```bash
yarn add @animate/heartbeat
```

or npm

```bash
npm i @animate/heartbeat --save
```

## `<Heartbeat />`

<Playground>
  <Heartbeat>
    <div
      style={{
        maxWidth: 300,
        boxShadow: "rgba(0, 0, 0, 0.2) 0px 0px 10px",
        backgroundColor: "white",
        textAlign: "center",
        borderRadius: 4,
        padding: 8,
        marginTop: 8,
        width: 300
      }}
    >
      <h1>React Component</h1>
    </div>
  </Heartbeat>
</Playground>
